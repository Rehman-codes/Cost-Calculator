import './finalprice.css';

function FinalPrice({ finalPrice }) {

    return (
        <>
            <section id='finalprice'>
                <h1>Rs. {finalPrice}</h1>
            </section>
        </>
    );
}

export default FinalPrice;
